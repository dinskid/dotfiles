STR_TO_NUM PROC
; EXPECTS SI TO HAVE OFFSET
MOV CL, [SI+1]
ADD SI,2

XOR BX, BX

MOO:
	MOV AX, BX
	MOV BX, 10
	MUL BX
	MOV BX, AX
	LODSB
	XOR AH, AH
	SUB AL, 30H
	ADD BX, AX
	LOOP MOO
	MOV AX, BX
	RET
STR_TO_NUM ENDP
